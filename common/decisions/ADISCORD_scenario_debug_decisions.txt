ADISCORD_scenario_debug_category = {
	ADISCORD_debug_start_vorkerland_collapse = {
		allowed = { tag = WRK }
		visible = { NOT = { has_global_flag = ADISCORD_vorkerland_collapse_started } }
		cost = 0
		complete_effect = { country_event = { id = ADISCORD_vorkerland_collapse.1 } }
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_open_vorkerland_dirty_zone = {
		allowed = { OR = { tag = WRK tag = WKR tag = VAD tag = TVA tag = RUS } }
		visible = {
			has_global_flag = ADISCORD_vorkerland_collapse_started
			NOT = { has_global_flag = ADISCORD_vorkerland_dirty_opened }
		}
		available = { RUS = { exists = yes } }
		cost = 0
		complete_effect = {
			# Preserve the real reveal contract: only skip the timer, then let .85
			# own the superevent and the staggered Dirty Zone country cascade.
			set_global_flag = ADISCORD_vorkerland_dirty_reveal_v2_scheduled
			RUS = { country_event = { id = ADISCORD_vorkerland_collapse.85 } }
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_add_vorkerland_war_exhaustion = {
		allowed = { OR = { tag = WKR tag = VAD tag = TVA } }
		visible = { has_global_flag = ADISCORD_vorkerland_collapse_started }
		cost = 0
		complete_effect = {
			add_to_variable = {
				var = ADISCORD_vorkerland_civil_war_exhaustion
				value = 25
			}
			clamp_variable = {
				var = ADISCORD_vorkerland_civil_war_exhaustion
				min = 0
				max = 100
			}
			ADISCORD_vorkerland_refresh_civil_war_exhaustion = yes
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_reset_vorkerland_war_exhaustion = {
		allowed = { OR = { tag = WKR tag = VAD tag = TVA } }
		visible = { has_global_flag = ADISCORD_vorkerland_collapse_started }
		cost = 0
		complete_effect = { ADISCORD_vorkerland_reset_civil_war_exhaustion = yes }
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_form_vorkerland_joint_government = {
		allowed = { tag = WRK }
		visible = {
			has_global_flag = ADISCORD_vorkerland_collapse_started
			NOT = { has_global_flag = ADISCORD_vorkerland_joint_government_formed }
			VAD = { exists = yes }
		}
		cost = 0
		complete_effect = { ADISCORD_vorkerland_form_joint_government = yes }
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_force_vorkerland_central_victory = {
		allowed = { OR = { tag = WKR tag = VAD tag = TVA } }
		visible = {
			has_global_flag = ADISCORD_vorkerland_collapse_wars_started
			NOT = { has_global_flag = ADISCORD_vorkerland_central_war_finished }
		}
		cost = 0
		complete_effect = {
			if = { limit = { tag = WKR } ADISCORD_vorkerland_apply_worker_map = yes }
			else_if = { limit = { tag = VAD } ADISCORD_vorkerland_apply_vlad_map = yes }
			else_if = { limit = { tag = TVA } ADISCORD_vorkerland_apply_dorian_map = yes }
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_open_ivanland_intervention = {
		allowed = { tag = IVN }
		visible = { NOT = { has_global_flag = ADISCORD_vorkerland_ivanland_intervention_resolved } }
		cost = 0
		complete_effect = {
			if = {
				limit = { NOT = { has_global_flag = ADISCORD_vorkerland_collapse_started } WRK = { exists = yes } }
				WRK = { country_event = { id = ADISCORD_vorkerland_collapse.1 } }
			}
			set_global_flag = ADISCORD_vorkerland_ivanland_intervention_available
			set_global_flag = ADISCORD_vorkerland_claim_wars_authorized
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_resolve_ivanland_success = {
		allowed = { tag = IVN }
		visible = { has_global_flag = ADISCORD_vorkerland_ivanland_intervention_active }
		cost = 0
		complete_effect = { ADISCORD_vorkerland_ivanland_intervention_success = yes }
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_resolve_ivanland_failure = {
		allowed = { tag = IVN }
		visible = { has_global_flag = ADISCORD_vorkerland_ivanland_intervention_active }
		cost = 0
		complete_effect = { ADISCORD_vorkerland_ivanland_intervention_failure = yes }
		ai_will_do = { factor = 0 }
	}

	ADISCORD_nam_debug_start_resource_war = {
		allowed = { OR = { tag = NAM tag = EFL tag = AZH tag = SLF } }
		visible = { NOT = { has_global_flag = ADISCORD_nam_resource_war_started } }
		cost = 0
		complete_effect = { ADISCORD_nam_debug_start_resource_war = yes }
		ai_will_do = { factor = 0 }
	}

	ADISCORD_nam_debug_resolve_coalition_victory = {
		allowed = { OR = { tag = NAM tag = EFL tag = AZH tag = SLF } }
		visible = { has_global_flag = ADISCORD_nam_resource_war_started }
		cost = 0
		complete_effect = { ADISCORD_nam_debug_resolve_coalition_victory = yes }
		ai_will_do = { factor = 0 }
	}

	ADISCORD_nam_debug_resolve_nam_victory = {
		allowed = { OR = { tag = NAM tag = EFL tag = AZH tag = SLF } }
		visible = { has_global_flag = ADISCORD_nam_resource_war_started }
		cost = 0
		complete_effect = { ADISCORD_nam_debug_resolve_nam_victory = yes }
		ai_will_do = { factor = 0 }
	}

	# War diagnostics use native occupation and diplomacy, never fake capitulation.
	ADISCORD_debug_war_log_on = {
		icon = generic_prepare_civil_war
		visible = { is_debug = yes is_ai = no NOT = { has_global_flag = ADISCORD_war_debug_logging } }
		available = { always = yes }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				set_global_flag = ADISCORD_war_debug_logging
				log = "[ADISCORD_PEACE] trace enabled"
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_log_off = {
		icon = generic_prepare_civil_war
		visible = { is_debug = yes is_ai = no has_global_flag = ADISCORD_war_debug_logging }
		available = { always = yes }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				clr_global_flag = ADISCORD_war_debug_logging
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_snapshot = {
		icon = generic_prepare_civil_war
		visible = { is_debug = yes is_ai = no }
		available = { always = yes }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				ADISCORD_debug_war_snapshot = yes
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_declare = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { has_capitulated = no is_subject = no NOT = { has_war_with = FROM is_in_faction_with = FROM } FROM = { has_capitulated = no is_subject = no } }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				declare_war_on = { target = FROM type = annex_everything }
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_join_faction = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { is_subject = no is_in_faction = no has_capitulated = no NOT = { has_war_with = FROM } FROM = { has_capitulated = no OR = { is_subject = no is_in_faction = yes } } }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				FROM = {
				    if = { limit = { is_in_faction = no } create_faction_from_template = { template = faction_template_ADISCORD_standard name = ADISCORD_war_debug_faction } }
				    faction_leader = { add_to_faction = ROOT }
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_join_wars = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { has_capitulated = no is_subject = no is_in_faction_with = FROM FROM = { has_war = yes } }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				FROM = {
				    every_enemy_country = {
				        limit = { NOT = { tag = ROOT is_in_faction_with = ROOT has_war_with = ROOT } }
				        ROOT = { add_to_war = { targeted_alliance = FROM enemy = PREV hostility_reason = asked_to_join single_target_only = yes } }
				    }
				}
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_occupy_capital = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { has_capitulated = no has_war_with = FROM }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				FROM = { capital_scope = { set_state_controller_to = ROOT } }
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_occupy_country = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { has_capitulated = no has_war_with = FROM }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				FROM = { every_owned_state = { set_state_controller_to = ROOT } }
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_liberate = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { has_war_with = FROM }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				FROM = { every_owned_state = { limit = { OR = { is_controlled_by = ROOT controller = { is_subject_of = ROOT } } } set_state_controller_to = PREV } }
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_white_peace = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { has_war_with = FROM }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				white_peace = FROM
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_major_on = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { FROM = { is_major = no NOT = { has_country_flag = ADISCORD_war_debug_added_major } } }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				FROM = { set_country_flag = ADISCORD_war_debug_added_major set_major = yes }
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_major_off = {
		icon = generic_prepare_civil_war
		targets = { VAL CIN OSF APH STP STS SRP NOD YPR COF TFF AIN WRK WKR VAD TVA IVN RUS SLA RZA MLR ERT IRT SCA NAM EFL AZH SLF }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no }
		available = { FROM = { has_country_flag = ADISCORD_war_debug_added_major } }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				FROM = { set_major = no clr_country_flag = ADISCORD_war_debug_added_major }
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_val_start = {
		icon = generic_prepare_civil_war
		targets = { CIN OSF APH ERT }
		target_trigger = { FROM = { exists = yes NOT = { tag = ROOT } } }
		visible = { is_debug = yes is_ai = no tag = VAL }
		available = { VAL_frontier_postwar = yes VAL_frontier_idle = yes has_war = no FROM = { has_capitulated = no is_subject = no NOT = { is_in_faction_with = ROOT } } }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				if = { limit = { FROM = { tag = CIN } } set_variable = { var = VAL_frontier_target value = 1 } }
				else_if = { limit = { FROM = { tag = OSF } } set_variable = { var = VAL_frontier_target value = 2 } }
				else_if = { limit = { FROM = { tag = APH } } set_variable = { var = VAL_frontier_target value = 3 } }
				else = { set_variable = { var = VAL_frontier_target value = 4 } }
				if = {
				    limit = { VAL_frontier_prewar_eligible = yes }
				    set_variable = { var = VAL_frontier_stage value = 2 }
				    VAL_frontier_start_war = yes
				}
				else = { log = "[ADISCORD_PEACE] frontier debug entry rejected: ownership/control or sovereignty changed" }
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_val_check = {
		icon = generic_prepare_civil_war
		visible = { is_debug = yes is_ai = no tag = VAL }
		available = { check_variable = { var = VAL_frontier_stage value = 0 compare = greater_than } }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				VAL_frontier_weekly = yes
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_val_abort = {
		icon = generic_prepare_civil_war
		visible = { is_debug = yes is_ai = no tag = VAL }
		available = { check_variable = { var = VAL_frontier_stage value = 0 compare = greater_than } }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				VAL_frontier_close = yes
			}
		}
		ai_will_do = { factor = 0 }
	}

	ADISCORD_debug_war_reserves = {
		icon = generic_prepare_civil_war
		visible = { is_debug = yes is_ai = no }
		available = { always = yes }
		cost = 0
		fire_only_once = no
		complete_effect = {
			if = {
				limit = { is_debug = yes is_ai = no }
				add_manpower = 50000
				add_equipment_to_stockpile = { type = infantry_equipment amount = 10000 producer = ROOT }
			}
		}
		ai_will_do = { factor = 0 }
	}
}

# BEGIN CONSOLIDATED: ADISCORD_test_wars_decisions.txt
ADISCORD_test_wars_category = {
	APH_start_test_war_with_OSF = {
		visible = {
			tag = APH
			has_country_flag = ADISCORD_aph_osf_test_war_decision_unlocked
			NOT = { has_global_flag = ADISCORD_aph_osf_test_war_active }
			NOT = { has_global_flag = ADISCORD_aph_osf_test_war_finished }
		}

		available = {
			OSF = { exists = yes }
			NOT = { has_capitulated = yes }
			OSF = { NOT = { has_capitulated = yes } }
			NOT = { is_in_faction_with = OSF }
			NOT = { has_war_with = OSF }
		}

		cost = 0

		complete_effect = {
			set_global_flag = ADISCORD_aph_osf_test_war_active
			clr_global_flag = ADISCORD_aph_osf_test_war_finished
			clr_country_flag = ADISCORD_aph_osf_test_war_decision_unlocked

			OSF = {
				if = { limit = { is_in_faction_with = APH } leave_faction = yes }
			}

			declare_war_on = { target = OSF type = annex_everything }
		}

		ai_will_do = { factor = 1 }
	}
}
# END CONSOLIDATED: ADISCORD_test_wars_decisions.txt
