STP_elections_in_the_party = {

	STP_test = {
		icon = generic_political_pressure
		allowed = { tag = STP }
		visible = {
		}
		available = {
		}
		cost = 0
		days_remove = 0
		days_re_enable = 0
		complete_effect = {
			add_dynamic_modifier = { modifier = STP_party_suspicion_dynamic_modifier }
		}
		remove_effect = {
			set_variable = {
				var = STP_party_suspicion_rate
				value = 0.05
			}

			set_temp_variable = {
				var = STP_party_suspicion_rate_temp
				value = 0.05
			}

			STP_change_party_suspicion = yes
		}
	}
}