name: STP onboarding QoL verification

on:
  push:
    branches:
      - codex/stp-onboarding-qol-20260922

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Run focused Stelander regression suite
        run: |
          python -B -m unittest \
            tools.tests.test_adiscord_stp_preparation \
            tools.tests.test_adiscord_stp_civil_war \
            tools.tests.test_adiscord_stp_party_route -v
      - name: Run total-conversion validator
        run: python -B tools/validate_tc.py --limit 300
